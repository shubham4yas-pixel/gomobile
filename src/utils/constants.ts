/**
 * Shared constants for Beta Stabilisation Phase.
 */

// Fare estimate constants (mirroring backend server.js)
export const BASE_FARE = 3.0;
export const PER_KM_RATE = 1.5;

// Mock OTP for testing passenger verification
export const DEV_OTP = '4829';

// Driver incoming request countdown
export const INCOMING_REQUEST_COUNTDOWN_SEC = 15;
